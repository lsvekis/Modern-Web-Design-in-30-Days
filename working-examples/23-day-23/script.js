const topics=[{title:"HTML",description:"Structure"},{title:"CSS",description:"Style"},{title:"JavaScript",description:"Behavior"}];const area=document.querySelector("#topics");function render(filter="all"){area.innerHTML="";topics.filter(t=>filter==="all"||t.title===filter).forEach(t=>{const a=document.createElement("article");a.innerHTML=`<h2>${t.title}</h2><p>${t.description}</p>`;area.appendChild(a);});}document.querySelectorAll("[data-filter]").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll("[data-filter]").forEach(x=>x.classList.remove("active"));b.classList.add("active");render(b.dataset.filter);}));render();
